{
  "name": "growvia-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build && node -e \"import fs from 'node:fs'; fs.mkdirSync('dist', {recursive: true}); fs.cpSync('frontend/dist', 'dist', {recursive: true}); fs.mkdirSync('frontend/frontend', {recursive: true}); fs.cpSync('frontend/dist', 'frontend/frontend/dist', {recursive: true});\"",
    "backend:dev": "npm --prefix backend run dev",
    "backend:build": "npm --prefix backend run build",
    "build": "npm run frontend:build"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
